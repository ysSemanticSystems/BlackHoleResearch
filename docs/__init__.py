"""docs — data-bearing helper package.

This is *not* a code-quality home; modules here exist so reproducible-data
artifacts (literature SED values, calibration constants, etc.) can be
versioned and unit-tested without contaminating the analysis package.
"""
